<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-200">
    <nav class="md:mx-5 mx-2">
      <div class="px-2 p-3 sm:p-5 md:px-10 mt-3 md:mt-5 text-gray-700 shadow rounded-xl max-w-7xl select-none flex justify-between m-auto">
        <h1 class="sm:font-bold font-semibold sm:text-lg text-base my-auto"><%= link_to "Private Events", root_path %></h1>
        <ul class="flex md:gap-6 gap-3">
          <% if user_signed_in? %>
            <li class="hover:text-gray-900 hover:scale-110 transition-all md:block hidden m-auto"><%= link_to "Events", event_index_path %></li>
            <li class="hover:text-gray-900 hover:scale-110 transition-all m-auto"><%= link_to "Profile", profile_index_path %></li>
            <li class="m-auto"><%= link_to '<button class="bg-sky-500 hover:bg-sky-600 text-gray-100 font-semibold py-2 px-4 border border-sky-400 rounded shadow">
              New Event
            </button>'.html_safe, new_event_path %></li>
          <% else %>
            <li class="m-auto"><%= link_to '<button class="bg-white hover:bg-gray-100 text-gray-700 font-semibold py-2 px-4 border border-gray-400 rounded shadow">
                Sign In
              </button>'.html_safe, new_user_session_path %></li>
            <li class="m-auto"><%= link_to '<button class="bg-sky-500 hover:bg-sky-600 text-gray-100 font-semibold py-2 px-4 border border-sky-400 rounded shadow">
              Sign Up
            </button>'.html_safe, new_user_registration_path %></li>
          <% end %>
        </ul>
      </div>
    </nav>
    <main class="container mx-auto">
      <div class="h-8 mt-12 m-auto">
        <p class="notice text-center max-w-xl m-auto text-indigo-300"><%= notice %></p>
        <p class="alert text-center max-w-xl m-auto text-yellow-600 font-bold"><%= alert %></p>
      </div>
      <%= yield %>
    </main>
  </body>
</html>


 <%= button_to "Sign out", destroy_user_session_path, method: :delete %>

